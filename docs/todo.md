+ Add an enable_prints and disable_prints() API so the library can choose to print helpful <pending\> messages? maybe not?
+ Make it work cross-arch
+ Make it work for 32-bit and 64-bit (pwntools elf.py?)

